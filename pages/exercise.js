/* import { useState } from 'react';

function CartCounter({ cart, setCart }) {
  return (

    <div className="jh-cart">
      <div>
        {cart}
        <button onClick={() => setCart(cart - 1)}>➖</button>
        <button onClick={() => setCart(cart + 1)}>➕</button>{' '}
      </div>
    </div>
  );
}
export default function ExampleLiftingStateUp() {
  const [cart, setCart] = useState(0);

  return (
    <div>
      <CartCounter cart={cart} setCart={setCart} />
    </div>
  );
} */
